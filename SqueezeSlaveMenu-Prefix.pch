//
// Prefix header for all source files of the 'SqueezeSlaveMenu' target in the 'SqueezeSlaveMenu' project
//

#ifdef __OBJC__
  #import <Cocoa/Cocoa.h>

  #ifdef DEBUG
  #	define DLog(fmt, ...) NSLog((@"%s [Line %d] " fmt), __PRETTY_FUNCTION__, __LINE__, ##__VA_ARGS__);
  #else
  #	define DLog(...)
  #endif
#endif
